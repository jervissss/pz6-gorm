# Практическая работа №6: Использование ORM (GORM)
## 📋 Информация о работе
### Группа: ЭФМО-02-25
### Студент: Евдоков Богдан Вадимович

## 🎯 Что умеет этот сервер?
Этот сервер — пример веб-приложения на Go с использованием ORM GORM для работы с базой данных PostgreSQL. Сервер умеет:


1. Автоматически создавать таблицы в базе данных на основе Go-структур


2. Работать со связями между таблицами: 1:N (один ко многим) и M:N (многие ко многим)


3. Предоставляет REST API для работы с пользователями и заметками:

   
* `POST /users` — создание нового пользователя
* `POST /notes` — создание заметки с тегами
* `GET /notes/{id}` — получение заметки с информацией об авторе и тегах
* `GET /health` — проверка работоспособности сервера

## 📁 Структура проекта

<img width="358" height="428" alt="image" src="https://github.com/user-attachments/assets/7569f7d3-da2f-4681-a22f-54c80d789582" />


## 🛠️ Предварительные требования
Перед запуском убедитесь, что у вас установлено:

1. Go (версия 1.21 или выше) — скачайте с официального сайта

2. PostgreSQL (версия 15 или выше) — скачайте с официального сайта

3. Git (опционально) — для клонирования репозитория

## 🚀 Пошаговая инструкция по установке и запуску (Windows)
### Шаг 1: Создайте базу данных в PostgreSQL
1. Откройте PowerShell от имени администратора

2. Выполните команды:

```powershell
# Подключитесь к PostgreSQL
psql -U postgres

# Введите пароль (по умолчанию часто "postgres")
# Создайте базу данных
CREATE DATABASE pz6_gorm;

# Выйдите из psql
\q
```
## Шаг 2: Скачайте и настройте проект
```powershell
# 1. Скачайте проект (если используете Git)
git clone https://github.com/jervissss/pz6-gorm
cd pz6-gorm

# 2. Или создайте проект вручную
mkdir pz6-gorm
cd pz6-gorm
```

## Шаг 3: Установите зависимости
```powershell
# Инициализируйте Go-модуль
go mod init example.com/pz6-gorm

# Установите необходимые библиотеки
go get gorm.io/gorm gorm.io/driver/postgres github.com/go-chi/chi/v5
```
## Шаг 4: Настройте подключение к базе данных
```powershell
# Установите переменную окружения (PowerShell)
$env:DB_DSN = "host=localhost user=postgres password=postgres dbname=pz6_gorm port=5432 sslmode=disable"

# Если хотите сохранить навсегда (для всех сессий)
setx DB_DSN "host=localhost user=postgres password=postgres dbname=pz6_gorm port=5432 sslmode=disable"
```
Важно: Замените password=postgres на ваш реальный пароль от PostgreSQL.

## Шаг 5: Запустите сервер
```powershell
# Перейдите в папку с проектом
cd pz6-gorm

# Запустите сервер
go run ./cmd/server/main.go
```
Если все настроено правильно, вы увидите сообщение:

```text
listening on :8080
```
# 📡 Тестирование API
## Способ 1: Через PowerShell (рекомендуется для Windows)
Откройте новое окно PowerShell (старое не закрывайте — там работает сервер) и выполните:

### 1. Проверка работоспособности сервера
```powershell
Invoke-RestMethod -Uri "http://localhost:8080/health"
```
### Ожидаемый ответ:

```json
{"status": "ok"}
```
### 2. Создание пользователя
```powershell
$body = @{
    name = "Алиса"
    email = "alice@example.com"
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost:8080/users" -Method Post -ContentType "application/json" -Body $body
```
### Ожидаемый ответ:

```json
{
  "ID": 1,
  "Name": "Алиса",
  "Email": "alice@example.com",
  "CreatedAt": "2025-12-02T10:00:00Z",
  "UpdatedAt": "2025-12-02T10:00:00Z"
}
```
### 3. Создание заметки с тегами
```powershell
$body = @{
    title = "Первая заметка"
    content = "Это моя первая заметка с использованием GORM"
    userId = 1
    tags = @("go", "gorm", "postgres")
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost:8080/notes" -Method Post -ContentType "application/json" -Body $body
```

### 4. Получение заметки с автором и тегами
```powershell
Invoke-RestMethod -Uri "http://localhost:8080/notes/1" -Method Get
```
### Ожидаемый ответ (пример):

```json
{
  "ID": 1,
  "Title": "Первая заметка",
  "Content": "Это моя первая заметка с использованием GORM",
  "UserID": 1,
  "User": {
    "ID": 1,
    "Name": "Алиса",
    "Email": "alice@example.com"
  },
  "Tags": [
    {"ID": 1, "Name": "go"},
    {"ID": 2, "Name": "gorm"},
    {"ID": 3, "Name": "postgres"}
  ]
}
```
## Способ 2: Через браузер
Для GET-запросов можно использовать браузер:

* http://localhost:8080/health
* http://localhost:8080/notes/1

## 📊 Структура базы данных
Таблицы
users — хранит информацию о пользователях

notes — хранит заметки пользователей

tags — хранит теги для заметок

note_tags — таблица связей многие-ко-многим между заметками и тегами

Связи между таблицами
1:N (один ко многим): один пользователь → много заметок

M:N (многие ко многим): заметки ↔ теги (через таблицу note_tags)

### Как посмотреть структуру базы данных
```powershell
# Подключитесь к PostgreSQL
psql -U postgres -d pz6_gorm

# Просмотрите список таблиц
\dt

# Просмотрите структуру каждой таблицы
\d users
\d notes
\d tags
\d note_tags
```

## 📸 Скриншоты работы
1. Проверка работоспособности сервера
<img width="1919" height="1079" alt="Снимок экрана 2025-12-02 141856" src="https://github.com/user-attachments/assets/6978993f-a403-4188-9656-d2eb05022591" />

2. Создание пользователя
<img width="1919" height="1079" alt="Снимок экрана 2025-12-02 142637" src="https://github.com/user-attachments/assets/fb77c336-1a41-4343-a635-99f617241857" />

3. Создание заметки с тегами
<img width="1919" height="1079" alt="Снимок экрана 2025-12-02 143623" src="https://github.com/user-attachments/assets/7bdb8a79-08b8-445a-a6b2-e7bf146adf0b" />

4. Получение заметки с автором и тегами
<img width="1739" height="1079" alt="Снимок экрана 2025-12-02 143921" src="https://github.com/user-attachments/assets/e735a445-4571-428f-8538-ad5148ca4b37" />
